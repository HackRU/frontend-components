import React, { Component } from "react";
import { Button, GlowButton } from "lcs";

export default class App extends Component {
    render() {
        return (
            <div>
                <Button> Modern React component module </Button>
                <GlowButton />
            </div>
        )
    }
}
